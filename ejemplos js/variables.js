const nombre="misty"//no se puede asignar una constante
let contador=0;//un let si se puede reasignar
contador+=1;
console.log(nombre, contador);

const productos =["pokeballs"]
productos.push("super pocion")
console.log(productos)